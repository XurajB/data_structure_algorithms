package problems.string;

/**
 * We'd like you to implement a string interpolation algorithm. It's likely going to be used to format simple messages to be sent to users.
 * It should take a string and a dictionary of values. It will return the input string, but with the dictionary values substituted into the [] delimited tokens, for example:
 *
 * Example Case 1
 * input_string = "Hello [name]"
 * dictionary = {"name": "Jim"}
 *
 * Output:
 *
 * Hello Jim
 * Example Case 2
 * Escape the square brackets by doubling them
 *
 * input_string = "Hello [name] [[author]]"
 * dictionary = {"name": "Jim"}
 *
 * Output
 *
 * Hello Jim [author]
 */
public class StringInterpolation {

}
